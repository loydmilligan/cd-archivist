# Implement capture/rip pairing

Implement logic that pairs capture sessions with rip sessions.

Requirements:

- Pair primarily by timestamp proximity.
- Return confidence.
- Handle no match.
- Handle multiple plausible matches.
- Never silently choose an ambiguous match.
- Add tests.
