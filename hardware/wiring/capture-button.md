# Optional Capture Button

A manual capture button can be useful during early development.

Behavior:

```text
button press → create capture session → capture image burst
```

This avoids depending on tray detection while developing the rest of the pipeline.
